On the json file 

Configure the CONTAINERMODE to whatsapp.

Specify your WhatsApp Business API webhook URL and the Redis connection settings in the capabilities section of the botium.json file.

You will need to start an inbound proxy command with Botium CLI to ensure that messages from your chatbot are properly received.

This script is currently incomplete update configuration file before running the test.